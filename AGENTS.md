# 项目协作说明

这是一个本地运行的“毛泽东选集随机阅读器”。页面从 `article/` 目录中的 Markdown 文件读取文章，并以卡片分页形式展示正文和注释。

## 运行与验证

```bash
python3 main.py
```

服务默认监听 `http://127.0.0.1:8000`。修改页面或服务逻辑后，至少验证：

```bash
python3 -m py_compile main.py
curl -fsS http://127.0.0.1:8000/api/random
```

不要将长期运行的本地服务作为后台任务后立即退出；用户需要保持启动服务的终端窗口运行。

## 目录职责

```text
article/          文章 Markdown 数据源；不得改写、迁移或重命名
main.py           零依赖 Python HTTP 服务与 Markdown 解析
index.html        页面结构
static/app.js     随机加载、分页、注释渲染
static/style.css  视觉样式与响应式布局
static/*.png      页面使用的本地图片资源
```

## 实现原则

- 保持零第三方 Python 依赖；使用标准库即可完成的事情，不要引入 Flask、Node 或构建工具。
- 文章内容必须来自本地 `article/*.md`，不请求外部文本源，不复制文章到代码中。
- 将正文与“注释”分开解析；不要丢弃、拼接或篡改原始文章内容。
- 每次访问 `/` 必须随机展示一篇可用文章；“换一篇”也必须请求新的随机结果。
- 静态资源统一放在 `static/`，引用使用绝对站内路径，如 `/static/style.css`。
- 页面必须支持窄屏阅读，正文保持足够对比度和行距。
- 不要为了简单改动新增 helper 层、状态管理框架或前端构建链。

## 兼容性与安全

- 保持 `GET /`、`GET /api/random`、`GET /api/articles` 与 `GET /api/article/<id>` 的现有行为兼容。
- 文章文本在浏览器中渲染前必须转义 HTML；只允许受控的注释标记转换为展示元素。
- 限制文章 API 仅按已发现的本地文章 ID 查询，禁止将 URL 参数直接拼接为文件路径。
- 不修改用户已有的 `article/` 内容、Git 暂存内容或 IDE 配置。

## 提交前检查

- 确认页面在 `http://127.0.0.1:8000` 可加载。
- 确认随机文章接口返回 JSON，且正文和注释数量合理。
- 确认“换一篇”、分页和注释区域在浏览器中可用。
- 确认浏览器控制台没有脚本错误。
